# Mutation Engine

Mutate changed TypeScript only; compile-screen first; use strict budgets. Initial operators: boolean flip, comparison inversion, branch removal, boundary change, HTTP status swap, await removal, return substitution. Equivalent/surviving mutants are evidence gaps, not confirmed bugs.
