# Redaction fixture

This fixture exercises the runner’s secret-redaction path while still behaving like a normal intended-delta proof.
The probe emits a GitHub-style token-shaped string so the captured stdout can be verified as redacted.
